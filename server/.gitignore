# mutmut's mutation sandbox: a full copy of promptatron/ + tests/ per run,
# with per-mutant variants generated inside it. Disposable and regenerated by
# `uv run mutmut run`; never worth committing.
mutants/

# coverage.py's data file, when --cov-report leaves one behind locally.
.coverage
