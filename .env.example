# AWS Configuration for Bedrock LLM Analyzer
# Copy this file to .env and fill in your AWS credentials

# AWS Region (choose a region where Bedrock is available)
VITE_AWS_REGION=us-east-1

# AWS Credentials (for development only - not recommended for production)
# You can get these from AWS Console > IAM > Users > Security credentials
VITE_AWS_ACCESS_KEY_ID=your_access_key_here
VITE_AWS_SECRET_ACCESS_KEY=your_secret_key_here

# Optional: Session token (if using temporary credentials)
# VITE_AWS_SESSION_TOKEN=your_session_token_here

# Note: In production, use AWS Cognito Identity Pool or other secure methods
# Never commit actual credentials to version control