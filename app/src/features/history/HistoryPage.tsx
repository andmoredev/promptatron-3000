/**
 * Placeholder. The real run list (paging, filters, detail drawer) is built on
 * `historyStore` in a later work item.
 */

export default function HistoryPage() {
  return (
    <div className="card max-w-2xl mx-auto text-center" data-testid="history-page">
      <h2 className="text-lg font-semibold text-gray-900 mb-2">History</h2>
      <p className="text-sm text-gray-600">History — coming in this branch.</p>
    </div>
  )
}
