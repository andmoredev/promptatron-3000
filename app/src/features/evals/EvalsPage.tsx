/**
 * Placeholder. Determinism / grade evaluations are built on `evalStore` in a
 * later work item.
 */

export default function EvalsPage() {
  return (
    <div className="card max-w-2xl mx-auto text-center" data-testid="evals-page">
      <h2 className="text-lg font-semibold text-gray-900 mb-2">Evals</h2>
      <p className="text-sm text-gray-600">Evals — coming in this branch.</p>
    </div>
  )
}
