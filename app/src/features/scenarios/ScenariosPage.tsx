/**
 * Placeholder. Scenario authoring (prompts, datasets, tool definitions) is
 * built on `scenarioStore` + the config-store endpoints in a later work item.
 */

export default function ScenariosPage() {
  return (
    <div className="card max-w-2xl mx-auto text-center" data-testid="scenarios-page">
      <h2 className="text-lg font-semibold text-gray-900 mb-2">Scenarios</h2>
      <p className="text-sm text-gray-600">Scenarios — coming in this branch.</p>
    </div>
  )
}
