/**
 * Placeholder. Guardrail CRUD and version publishing are built on
 * `guardrailStore` in a later work item.
 */

export default function GuardrailsPage() {
  return (
    <div className="card max-w-2xl mx-auto text-center" data-testid="guardrails-page">
      <h2 className="text-lg font-semibold text-gray-900 mb-2">Guardrails</h2>
      <p className="text-sm text-gray-600">Guardrails — coming in this branch.</p>
    </div>
  )
}
