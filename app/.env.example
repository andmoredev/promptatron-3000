# Base URL of the Promptatron API server.
# The browser talks to nothing else — AWS credentials, Bedrock and any caching
# live server-side. Defaults to http://localhost:8000 when unset.
VITE_API_URL=http://localhost:8000
