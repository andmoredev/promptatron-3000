# AWS Configuration
VITE_AWS_ACCESS_KEY_ID=your_access_key_here
VITE_AWS_SECRET_ACCESS_KEY=your_secret_key_here
VITE_AWS_SESSION_TOKEN=your_session_token_here
VITE_AWS_REGION=us-east-1

# Momento Cache Configuration
# Optional: Set to your Momento API key to enable caching and rate limiting
# If not provided, caching and rate limiting will be disabled
VITE_MOMENTO_API_KEY=your_momento_api_key_here

# Cache Name Configuration
# Optional: Set the name of the Momento cache to use for caching operations
# Default: 'promptatron' (used if not specified)
# Example: Set to 'my-app-cache' for a custom cache name
# VITE_CACHE_NAME=my-app-cache

# App Name Configuration
# Optional: Set the app name prefix for localStorage keys
# Default: Uses VITE_CACHE_NAME or 'promptatron' if not specified
# Example: Set to 'my-app' to use 'my-app_form_state' instead of 'promptatron_form_state'
# VITE_APP_NAME=my-app

# Robot Debug Mode
# Set to 'true' to enable robot debug UI and console logging
# Useful for testing robot state transitions and debugging issues
VITE_ROBOT_DEBUG=false
